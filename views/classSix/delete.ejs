<%- include('../partials/header') %>

<section class="mainSection">
    <h2>Class Six</h2>
    <section class="mt-4">
        <h4>Delete this task?</h4>
        <% let date = taskSix.date %>
        <h5><%= taskSix.task %> - Due on <%= date.toString().substring(0,21) %></h5>
    </section>
    
    <form class="mt-4 text-center" action="/six/<%= taskSix._id %>?_method=DELETE" method="POST">
        <input class="btn btn-info" type="button" onclick="history.go(-1);" value="Back">    
        <input class="btn btn-success" type="submit" value="Submit">
    </form>
</section>

<%- include('../partials/footer') %>    