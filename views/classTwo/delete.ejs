<%- include('../partials/header') %>

<section class="mainSection">
    <h2>Class Two</h2>
    <section class="mt-4">
        <h4>Delete this task?</h4>
        <% let date = taskTwo.date %>
        <h5><%= taskTwo.task %> - Due on <%= date.toString().substring(0,21) %></h5>
    </section>
    
    <form class="mt-4 text-center" action="/two/<%= taskTwo._id %>?_method=DELETE" method="POST">
        <input class="btn btn-info" type="button" onclick="history.go(-1);" value="Back">    
        <input class="btn btn-success addOrDelete" type="submit" value="Submit">
    </form>
</section>

<%- include('../partials/footer') %>    